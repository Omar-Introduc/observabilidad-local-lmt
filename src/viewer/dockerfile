FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/viewer /app/src/viewer

CMD ["uvicorn", "src.viewer.main:app", "--host", "0.0.0.0", "--port", "8000"]